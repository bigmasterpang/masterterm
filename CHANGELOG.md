# MasterTerm 更新日志 (Changelog)

本文档记录了 MasterTerm 的历史版本更新与修复内容。

---

### v0.1.135 (2026-09-18)

- **RDP 原生右键菜单与 SSH 菜单项全面对齐与分屏项修复**：
  - **全量终端类型与批量执行对齐**：原生 Win32 右键菜单新增 PowerShell 7、Windows PowerShell、命令提示符 (CMD)、WSL (Linux)、Git Bash 以及批量执行命令选项，实现 RDP 激活状态下标签栏右键菜单与 SSH 状态下的 HTML 右键菜单 100% 体验对齐；
  - **修复关闭分屏项与菜单悬浮高亮**：纠正非分屏状态下“关闭分屏”误作为分隔线渲染的问题，正确将其呈现为禁用置灰项并禁止悬停高亮背景，彻底修复右键弹窗中出现双分隔线与文本缺失的缺陷。
- **SSH 跳板机配置新增“启用跳板机”开关选项**：
  - **开关控制与表单即时联动**：跳板机配置分组标题新增“启用跳板机 (ProxyJump)”独立复选框，未勾选时下方跳板机参数表单自动置灰并禁用输入；
  - **保留配置参数并支持随时切换**：取消勾选时完整保留已配置的跳板机地址、端口、用户、密钥与密码等凭据，无需重复输入；只有勾选启用时，系统才会通过跳板机代理路由 SSH、SFTP 与延迟探测，未启用时则直接连接目标主机。

### v0.1.134 (2026-09-18)

- **连接栏空白处右键菜单全面升级（完美支持原生 RDP 穿透交互）**：
  - **RDP 模式原生菜单委托（TrackPopupMenuEx）**：当当前激活会话为远程桌面时，连接栏空白处右键菜单自动委托至宿主底层 Win32 原生菜单，彻底突破 ActiveX 原生窗口空域限制（Airspace Issue），点击外部瞬间关闭且绝不遮挡；
  - **右键菜单新增打开远程桌面选项**：菜单中增加“打开远程桌面 (RDP)…”操作项，支持在标签栏空白处一键唤起并切换/连接配置好的 RDP 主机；
  - **全量快捷操作与连接排序**：支持新建默认本地终端、各类 Shell、SSH 终端、RDP 桌面、关闭全部、关闭分屏以及按名称/类型重新排序标签。
- **常用运维命令分类标签主题适配**：
  - **浅色与深蓝主题完美匹配**：修复常用命令分类徽标（`.snippet-category-badge`）在浅色模式下呈现深黑底色的对比度问题，适配浅色模式清爽浅蓝底色（`#eff6ff`）与深蓝模式海洋蓝底色（`#0b2a4a`），与整体主题高度协调。
- **侧边栏端口转发体验深度革新**：
  - **转发规则持久化与生命周期管理**：端口转发规则保存至本地存储，停止转发后规则不再消失，而是转入“已停止”状态，支持随时“启动”与“删除”，避免重复繁琐输入；
  - **新建转发右侧按钮功能就位**：补齐右侧刷新按钮的 SVG 矢量图标、居中排版与主题样式，点击时触发平滑旋转动效（`spin`）与“已刷新端口转发通道”轻提示，快速同步后端活跃隧道状态。

### v0.1.133 (2026-09-18)

- **终端底部状态栏与人机工学深度打磨**：
  - **状态栏内容左对齐呈现**：状态栏内容改为从左向右紧凑排布，最左侧为当前聚焦会话的彩色状态指示圆点与会话名称，紧接着是以精致竖线分割的实时延迟、CPU（含趋势波形图）、内存、网络、磁盘等指标，视觉流线更清晰自然；
  - **状态栏高度与字号黄金比例**：状态栏高度由 26px 调整为 **30px**，字号设为 12px，消除原先过于偏窄局促的感觉，更加饱满舒适；
  - **打满一屏终端真实垫高**：在终端容器上直接配置内边距规则，使终端自适应计算插件天然缩减 1.5 行高度，打满一屏时底部输入行稳定拥有约 28px 的充裕呼吸垫高空间，彻底消除贴底输入的视疲劳。
- **RDP 标签悬浮微卡片空域穿透与原生 Tooltip 彻底修复**：
  - **空域穿透绘制（Airspace Clipping）**：在宿主窗口管理层将 `#rdp-tab-hover-card` 纳入原生 RDP 子窗口的遮挡镂空区域，使 HTML 悬浮卡片能够在原生远程桌面上方清晰、完整地展示；
  - **清除原生 Tooltip 干扰**：彻底清理 RDP 标签上的原生 `title` 属性，杜绝系统原生黑白文字框遮挡自定义信息卡片；
  - **防抖与交互平滑过渡**：优化悬浮触发与隐藏防抖逻辑，支持鼠标从标签平滑滑入悬浮卡片内部进行查看，交互体验丝滑细腻。

### v0.1.132 (2026-09-18)

- **终端底部状态栏与人机工学优化（彻底解决贴底输入视疲劳）**：
  - **终端底部状态栏就位**：在终端视口下方新增 26px 专属状态栏，配合视口底部留白（`padding-bottom: 6px`），将打满一屏后的输入行向上垫高约 30px，极大缓解长时间低头贴底输入的颈椎与视疲劳；
  - **会话身份与指标清晰归属**：状态栏左侧显示当前激活会话的状态指示灯与名称（如 `● prod-server-1`）；右侧以胶囊化稳定布局实时展示网络延迟、CPU 使用率（含趋势波形图）、内存使用率、磁盘占用、网络 I/O 与运行时间；
  - **本地与串口终端统一体验**：针对本地终端（PowerShell / CMD / WSL）与串口终端，底栏整洁呈现会话类型、编码（UTF-8）与终端行列尺寸（如 `120×36`），保持界面专业一致。
- **顶部导航栏大幅瘦身与呼吸感释放**：
  - 将原先堆叠在右上角的复杂服务器指标彻底下移，顶部导航栏恢复清爽，彻底避免窗口偏窄时的挤占与布局换行。
- **RDP 远程桌面 100% 视口纯净独占（底栏智能收起）**：
  - 切换至 RDP 会话时，底部状态栏自动收起（高度归零），让远程桌面独占 100% 垂直空间，绝不遮挡远程 Windows 的任务栏和“开始”按钮。
- **RDP 标签悬浮连接信息微卡片（Hover Popover）**：
  - 鼠标悬停在 RDP 标签上（180ms 防抖）时，优雅浮现半透明磨砂卡片，按需展示远程 IP、实时网络往返延迟（ms）、画质模式、远程协商分辨率、网络连接类别（LAN/WAN）与色深状态；鼠标移出、点击或窗口失焦时迅速平滑隐去。

### v0.1.131 (2026-09-17)

- **弹窗关闭与失焦防误关交互体验优化**：
  - **精细化权限分流**：除**快捷跳转** (`Ctrl+P` / `Ctrl+K`) 与**检查更新**继续保留轻量级“失焦或点击外部自动关闭”特性外，其余所有表单与配置类弹窗（服务器配置、系统设置、快捷键速查、关于、常用命令及宏参数填写、端口转发、云端历史等）均调整为严格保护模式；
  - **杜绝背景误触丢失输入**：彻底消除编辑服务器连接或调整系统参数时因误点外部背景导致弹窗意外关闭并丢失已输入配置的问题；
  - **明确主动关闭机制**：非轻量弹窗仅在用户点击右上角关闭按钮（`✕`）、底部的取消/关闭/确定按钮，或主动按下键盘 `Esc` 键时才会关闭。

### v0.1.130 (2026-09-17)

- **解决 RDP 自动重连多端抢占死循环（防止互踢陷入无限循环）**：
  - **断开原因码精确识别与分类**：在底层 C++ 原生 ActiveX 事件处理中，准确读取 `DISPID_DISCONNECTED` 的 `discReason` 参数，区分会话被其它端接管（错误码 3、264）、远端注销（错误码 3334）、主动退出（错误码 1、2）及网络异常错误；
  - **前端抢占熔断机制**：当检测到会话是由另一台设备接入接管或注销时，前端立即销毁重连定时器并重置重试计数器，绝对禁止自动重连，并明确提示“检测到会话被其它设备接管，已停止自动重连以避免互相踢下线”；
  - **默认策略安全禁用**：将 RDP 连接配置中的自动重连默认策略调整为“默认 (禁用·防止多端登录冲突)”，同时保留“启用”和“跟随全局设置”供用户按需配置，从源头上杜绝多端并发使用时的抢占死循环。

### v0.1.129 (2026-09-16)

- **修复启动时 Temporal Dead Zone 引起的 ReferenceError 异常（解决启动时“界面加载超时：前端脚本未能启动”缺陷）**：彻底排查并修复 `resources/web/app.js` 顶层执行时访问未声明变量引发的 `ReferenceError: Cannot access 'updateSidebarCloudUi' before initialization`。在前置顶级作用域预声明相关函数变量并提升绑定，彻底杜绝前端启动崩溃。
- **启动流程全链路防御性加固**：
  - **组件容错隔离**：为侧边栏三大面板（运维命令、端口转发、云备份）初始化增加 `try-catch` 容错隔离，任何单个面板 DOM 或数据异常绝不阻塞主程序与启动画面的顺利呈现；
  - **启动兜底守护计时器**：新增 3.5 秒启动兜底守护机制，当后端首屏数据获取受阻或网络延迟时，主动释放启动遮罩并保证主界面可交互；
  - **看门狗异常捕获增强**：在 `index.html` 启动看门狗中捕获全局脚本运行时错误，出现异常时直接显示具体错误详情，避免误报“web 目录缺失”。
- **彻底解决 v0.1.126 客户端检查更新卡死问题**：
  - 排查确认旧版本客户端底层截断字节逻辑在处理超过 4000 字节的更新日志时切断中文字符编码导致序列化异常；
  - 发布更新流程严格约束 Release Notes 清单体积至 2800 字节以内，保障早期版本（含 v0.1.126）平滑检查与一键升级。

### v0.1.128 (2026-09-16)

- **最左侧工作栏新增三大核心运维面板（常用命令、端口转发、云备份）**：
  - **⚡ 常用运维命令库 (`snippets`)**：内置 70+ 生产级高频 Linux/DevOps 运维指令模板（打包解压、文件定位、文本搜索、系统监控、网络诊断、服务进程、Docker 容器），支持关键字实时搜索与分类筛选，提供**直接执行**（回车发送至终端）、**参数插入**（插入到终端输入行方便编辑）与**一键复制**，并支持自定义命令收藏与管理；
  - **🔀 端口转发中心 (`tunnels`)**：侧边栏原生集成 SSH 隧道管理，支持在已连接的 SSH 会话上可视化建立本地端口转发 (Local) 与远端反向隧道 (Remote)，实时监控转发状态、路由地址端点、活跃连接数，并支持一键停止；
  - **☁️ 云备份与快照历史 (`cloud`)**：将云端账号、配置同步与快照管理集成至侧栏。清晰呈现云端连接状态与上次同步时间，支持一键备份至云端、从云端恢复、最近快照版本预览与回滚，并提供本地离线备份文件的安全导出与一键恢复功能。
- **🖥️ RDP 远程桌面高级设置默认项语义明确化**：针对远程桌面全部关键设置（颜色深度、画质模式、智能缩放、网络连接类型、全屏键盘钩子、剪贴板重定向、音频传递、磁盘重定向等），全面补充并明确展示默认配置项的具体行为与中文语义，彻底消除模糊提示。
- **🔀 跳板机 (Jump Server) 独立端口输入与网关延迟探测**：
  - **配置区域统一**：将跳板机设置收拢为独立卡片，提供与上级主机一致的独立专用端口输入框，支持 `host:port` 智能自动推导解析；
  - **网关延迟探测**：重构延迟探测管线，当 SSH 连接启用跳板机时，自动将延迟探测目标定位为第一跳跳板服务器，前端延迟醒目标记 `ms (跳板)`，真实反映实际网络质量。

### v0.1.127 (2026-09-15)

- **彻底清除下线服务域名残留**：全量清理前端、配置迁移逻辑与文档中对旧域名 `ps.dapang.wang` 的历史残留与兼容判断，统一收拢为官方主服务地址 `https://vm.dapang.wang/cloud-sync`。
- **README.md 补充公益备份服务声明与私有化指引**：在官方文档中郑重声明 `vm.dapang.wang` 为开发者公益免费提供的配置同步与备份服务；详细说明其服务性质、安全隐私机制（基于 Windows DPAPI 硬件级本地加密，云端零知识存储密文）及自主私有化部署指引。
- **普通用户云端历史备份限额为 3 个（防止恶意滥用与存储耗尽）**：
  - **服务端加固**：重构 `cloud/cloud-sync.py` 角色与配额模型（普通用户限额 3 个，管理员限额 50 个）；在执行云端覆盖备份前，若已有快照备份数达到配额，严格阻断并返回友好中文错误提示，防范绕过前端进行高频无序刷量；
  - **前端交互拦截**：在客户端“上传到云端”发起前主动预检快照数量，达标时弹出对话框引导用户前往【历史版本】管理并删除旧版本；在【历史版本】面板新增配额指示器（`当前配额：X / 3`）及超额醒目告警。
- **全链路安全漏洞审计与恶意攻击防护**：
  - **超大 Payload 内存 DoS 防护**：在客户端 C++ 底层与服务端 Python 层双向拦截超过 5MB 的大包数据，杜绝超大 Payload 引起的内存暴涨与程序崩溃；
  - **私钥文件名路径穿越与 NTFS ADS 注入防护**：全面加固 C++ 后端 `importKeyData`，对私钥文件名实施严格白名单清洗（仅允许字母、数字、点、横杠与下划线），彻底杜绝 Windows 路径穿越、冒号注入与特殊字符篡改；
  - **算力耗尽 DoS 与暴力破解防护**：在云同步服务端引入基于 IP 的滑动窗口速率限制（Rate Limiting），对注册、登录和同步写入接口实施请求频率阈值控制，保护 PBKDF2 120,000 次哈希计算安全；
  - **HTTP 响应安全头加固**：服务端全面增加 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY` 及 `Cache-Control: no-store`。

### v0.1.126 (2026-09-15)

- **未分屏顶层终端切回分屏视图修复（解决鼠标点击“🔲 终端分屏”无响应问题）**：彻底修复当活动窗口处于未分屏顶层终端时，鼠标点击顶部“🔲 终端分屏”标签无法切回下方分屏视图的缺陷。统一抽象 `switchToSplitView()` 方法，点击时精准定位并激活分屏窗格中的活动会话，将顶层全屏独占状态切换回分屏并即时重绘左右/上下窗格，保持与 Ctrl+Tab 切换行为的一致性。
- **分屏窗格与顶层未分屏栏标签双向自由拖拽**：全面打通分屏模式下标签在分屏窗格（`leftGroup`/`rightGroup`）与顶层未分屏栏（`terminalTabs`）之间的双向拖拽。支持将分屏终端拖出至顶栏成为独立顶层终端（若单侧窗格拖空则自动优雅合并分屏）；同时支持将顶栏终端拖回分屏窗格。
- **检查更新弹窗历史日志保留最近 5 次优化**：优化版本发布说明体验。打包构建流程与前端呈现引擎全面联动，智能过滤并保留最近 5 次版本的更新日志，彻底消除以往展示长篇历史提交导致的冗长信息与视觉负担。
- **“🔲 终端分屏”专用右键上下文菜单与分屏标签右键功能增强**：为顶栏“🔲 终端分屏”标签绑定右键菜单，支持右键一键呼出“切回终端分屏”、“切换为左右/上下分屏”、“关闭分屏”、“批量执行命令”及“关闭全部”；同时修复分屏窗格内部会话标签的父级索引，恢复其右键菜单中“左移”与“右移”的操作能力。

### v0.1.125 (2026-09-15)

- **关于、检查更新、快速跳转与快捷键速查中心弹窗全量垂直居中**：统一系统模态弹窗在各种屏幕与视口下的居中规范（`top: 50%; left: 50%; transform: translate(-50%, -50%)`），修复以往在较高或较宽显示器下弹窗靠上（`top: 10vh`/`12vh`）且与下方交互不对齐的视觉体验。
- **独立检查更新弹窗布局与底部按钮自适应修复**：解耦 `.action-dialog` 遗留定宽定高样式冲突，重构 `.update-container` 弹性布局，确保更新弹窗底部“重新检查”、“关闭”及“立即更新”操作按钮在各主题与长日志内容下始终完整可见且自适应固定在底部；补全弹窗内当前版本与最新版本版本号 `vX.Y.Z` 规范化展示，并在已经是最新版本时清晰展示状态说明。
- **分屏模式下顶层未分屏栏新建独立终端会话**：重构分屏状态下的连接宿主解析逻辑 `resolveTabGroupHost` 与会话分发机制。在终端分屏（下方左右分屏）且顶部存在未分屏连接栏时，用户通过顶栏“`+`”或快速连接栏新建连接，将精准作为未分屏独立终端创建在顶栏中，切换时可全屏独占展现，点击“🔲 终端分屏”即可无缝切回分屏视图，不再错误挤入左侧分屏窗格。

### v0.1.124 (2026-09-15)

- **恢复 RDP 会话原生 Win32 系统菜单代理（解决 ActiveX 异步渲染遮挡与残影）**：回滚 v0.1.123 尝试在 RDP 视图下通过 Web 区域打洞渲染 HTML 下拉菜单的改动，恢复 v0.1.122 经过实测验证的 `TrackPopupMenuEx` 原生 Win32 菜单机制。在激活 RDP 远程桌面时，“🛠️ 工具 ▾”与“❓ 帮助 ▾”稳定呼出最高层级系统级菜单，彻底避免底层 ActiveX (`mstscax.dll`) Direct3D 硬件加速绘制造成的画面裁切、黑边残影及点击穿透问题。
- **保留独立关于与检查更新弹窗**：继续保持专有的“关于 MasterTerm”（包含项目发布平台 `https://master.dapang.wang` 介绍与一键直达）与“检查更新…”独立弹窗交互。

### v0.1.122 (2026-09-15)

- **RDP 窗口工具与帮助菜单 Win32 原生层级代理（解决 ActiveX 遮挡 BUG）**：彻底修复在激活 RDP 远程桌面会话时，点击顶部“🛠️ 工具 ▾”和“❓ 帮助 ▾”下拉菜单被原生 Win32 ActiveX 画面严重遮挡无法显示的缺陷。参考主题菜单模式，在检测到处于活动 RDP 视图时，自动向 C++ 后端发起 `app.toolsMenu` 与 `app.helpMenu` 原生调用，通过 `TrackPopupMenuEx` 弹出最高层级的原生 Win32 上下文菜单；完美支持广播输入状态打勾指示、端口转发、批量执行、云同步、诊断日志、快捷键速查、检查更新与关于弹窗等全量功能响应。
- **独立“关于 MasterTerm”专有弹窗与项目发布平台直达**：全面解耦以往点击“关于 MasterTerm”跳转系统设置底部的遗留逻辑。新增设计现代、磨砂毛玻璃质感的专用关于弹窗，精练介绍 MasterTerm 基于原生 Win32+WebView2 的架构特性与功能矩阵；并设立“官方项目与发布平台”专区，隆重介绍作者项目发布平台 `https://master.dapang.wang`，支持一键在系统默认浏览器中打开。
- **独立“检查更新”专有弹窗与设置页更新功能保留**：将“检查更新…”从设置页重定向解耦为独立专属弹窗，弹窗打开时自动发起版本检测，支持当前版本与最新版本对照、变更说明滚动预览、动态下载百分比进度条与一键安全重启安装；同时完整保留系统设置页内部的更新检测模块，双轨并行满足不同使用习惯。

### v0.1.121 (2026-09-15)

- **子命令严格匹配过滤与打分隔离（解决 systemctl status 显示 daemon-reload 问题）**：彻底修复当用户输入子命令（如 `systemctl status`）时，补全列表首行错误高亮显示不相关子命令（如 `systemctl daemon-reload`）的缺陷。在模板匹配引擎 `evaluateTemplateMatch` 中加入严格子命令位置校验，凡是位置参数（如 `status`、`restart`、`daemon-reload` 等）与模板字面量不匹配且非占位符的模板直接剔除返回 `null`，确保输入 `systemctl status` 时仅精准匹配并高亮展示 `systemctl status <service>`。
- **Ctrl+C / Ctrl+U / Ctrl+L 终端换行脱节与状态即时重置**：彻底修复在输入前缀命令（如 `system`）后按下 `Ctrl+C` 导致 Shell 换行后补全状态错乱（如补全为 `temctl status`）的问题。在前端底层按键监听与 xterm 数据流通道中全面拦截终端中断信号（`\x03` / Ctrl+C）、行首删除（`\x15` / Ctrl+U）、清屏重绘（`\x0c` / Ctrl+L）以及挂起中断（`\x1a` / Ctrl+Z），立即清空所跟踪的命令行缓存 `commandLine`、光标偏移与活动分段模板，并即时隐匿补全浮层，实现与远程/本地终端提示符状态的绝对一致。

### v0.1.120 (2026-09-15)

- **选中特定长选项模板一次性完整补全（解决 find /root 只补全到 -type 问题）**：彻底修复当用户在补全列表中选中带有多个后续参数的特定模板（如 `find /root -type f -mtime -1`）时，按下方向右键仅粗暴截取首个 Token（`-type`）而丢失后续参数的缺陷。重构为“整块连续参数扫描”，将所选模板中直至下一个未填占位符前的所有固定选项（如 ` -type f -mtime -1`）一次性完整追加至命令行，用户所见即所得。
- **纯占位符参数严格保护与 systemctl status frps 污染修复**：修复在输入 `systemctl status frps` 后触发补全被错误追加 `service.service` 的恶性 BUG。全面规范 `systemctl` 系列模板为通用的 `<service>` 纯占位符定义（遵循 Linux 最佳实践）；严密重构占位符匹配与增量生成引擎，纯占位符（如 `<service>`、`<path>`、`<container>` 等）必须由用户主动输入具体参数名，引擎绝不会将占位符名称本身作为字符输入终端，已完全填入参数的模板输出空增量，杜绝任何语法污染。

### v0.1.119 (2026-09-15)

- **终端屏幕缓冲区动态双向同步（解决 ArrowUp 历史召回脱节问题）**：彻底修复执行过长命令（如 `find /root -type f -mtime -7`）后，通过方向上键 `↑` 调出历史命令并退格删除部分内容（如变为 `find /root -type f `）时按方向右键 `→` 无反应的问题。新增 `getTerminalBufferLineInfo` 动态读取 xterm.js 屏幕行与光标位，自适应匹配标准 Shell Prompt 提取光标前实际输入，在按键交互及退格时实时纠偏终端输入状态。
- **常用模板严格优先置顶排序**：在命令补全候选项排序算法中强化 `isTemplate` 约束，确保系统内置的高频常用命令模板（如 `find`、`ps`、`journalctl`、`tar` 等）稳定置顶展示在候选框最上方，用户历史输入过的命令置于下方。
- **整段固定内容完整追加至首个占位符（ps -> ps -ef | grep ）**：重构模板分段补全引擎。当用户输入首词或命令前缀时（如 `ps`），方向右键不再单步推进单个参数，而是一次性将该分支直至占位符前的所有固定内容（` -ef | grep `）完整补全并直接进入待填入进程名状态；支持后续多段连续固定参数（如 `tar -xzvf <file> -C <dir>`）在输入文件名后继续连贯步进。
- **前缀匹配整段补全（journal -> journalctl --since "）**：优化命令前缀高亮候选的右键补全体验。输入 `journal` 时若高亮选中 `journalctl --since "<today>" -u <service>`，按下方向右键直接一次性补全整个固定前缀（`ctl --since "`），无需再次重新唤出选择。

### v0.1.118 (2026-09-15)

- **方向右键（`→`）连贯分段步进与退格回退保护**：彻底移除补全接收器中退格擦除历史输入的遗留逻辑。当候选已完全输入完毕（增量为空）时安全退出浮层，绝不发送 `\x7f` 将命令行重置为 `find`；按 `→` 键补全当前片段（如 `-ty` -> `-type `）后，浮层不再突兀消失，而是自动无缝刷新为下一步可用参数候选（如 `f` / `d`），实现平滑连贯的流式补全。
- **全流程多级参数链与 `-mtime` / `-size` 深度支持**：解决输入 `find /root -type f -mtime` 后按右键没有相关补全命令的问题。内置模板扩充 `-mtime -7`（最近7天）、`-mtime -1`（最近24小时）、`-mtime +30`（30天前）、`-mmin -60`（最近1小时）以及 `-size +100M`、`-size +1G` 等高频实用选项；从占位符中自动剥离有害的 `<>` 尖括号，防止在 Shell 中产生语法重定向报错；已完全匹配的模板自动降权，优先推进含后续可用参数的高阶模板。

### v0.1.117 (2026-09-15)

- **Tab 键职责分流与 Linux 原生 Shell 路径补全恢复**：彻底修复按 `Tab` 键时未阻止 Chromium 默认行为导致焦点跳跃到顶栏菜单以及 xterm 吞掉制表符的问题。默认将 `\t` (ASCII 0x09) 原生透传至远程/本地 Shell，恢复 Linux 极速路径补全（如 `find /ro` + `Tab` 自动补齐为 `/root/`）与文件名补全；仅在用户主动通过上下方向键显式高亮选中模板候选项时，`Tab` 与 `Enter` 才作为模板确认键，实现“原生 Shell 补全与智能模板”零干扰共存。
- **全参数词法分析与精准多 Token 智能打分引擎**：重构模板推荐打分器，解决输入 `find /root -type` 强行推荐无关 `find . -name "*.log"` 的匹配缺陷。引入词法 Token 提取与选项约束校验，凡是不包含用户已输入选项（如 `-type`）的模板一律剔除；优先置顶最契合的模板（如 `find <path> -type f` 普通文件、`find <path> -type d` 目录）；智能对齐参数槽位，将用户输入的实际路径（如 `/root`）自动映射至 `<path>` 占位符。
- **方向右键（`→`）精准增量追加与无损补全**：方向右键补全从“整行退格覆盖”优化为“增量差异追加”。用户键入 `find /root -type` 后按下 `→` 键，引擎精准计算缺失后缀（` f`）并直接向终端追加输出，已有输入绝不丢失；在键入 `tar -xzvf my_data.tar.gz` 后按 `→` 键，无缝追加后续分段 ` -C `，全流程丝滑流畅。

### v0.1.116 (2026-09-14)

- **常用命令分段补全与占位符解析（Segmented Completion）**：针对长命令补全时一次性填充完整示例导致用户需频繁退格修改的问题，引入 `<placeholder>` 智能分段语法体系。输入命令前缀时优先补全首段固定指令（例如 `tar -xzvf `），用户键入文件名后，再次按下方向右键（`→`）或 `Tab` 键即可秒级追加后续选项片段（如 ` -C `），大幅提升复杂命令输入效率。
- **内置运维命令与常用用法大幅扩充**：常用模板库扩充至 70+ 个高频生产级指令，深度覆盖 `tar`、`zip`、`find`、`grep`、`docker`、`docker compose`、`systemctl`、`journalctl`、`ss`、`netstat`、`lsof`、`tcpdump`、`ps`、`kill`、`pkill`、`curl`、`wget`、`chmod`、`chown`、`chattr`、`rsync`、`scp`、`df`、`du`、`git`、`sed`、`awk`、`tail`、`ufw`、`firewalld`、`crontab` 等 18 大核心领域，并以专属彩色徽标高亮占位符。
- **顶部菜单栏层级优化与二级下拉重构**：梳理原有扁平堆叠的 7+ 个异构按钮，重构为结构明晰的层级式导航栏。设立专属 `🔍 快速跳转 (Ctrl+P)` 主入口与 `⚙️ 设置 (Ctrl+,)`；将广播输入、端口转发、批量执行、云同步与诊断日志统一收纳至 `🛠️ 工具 ▾` 二级下拉菜单（广播激活时显示动态发光呼吸点）；将快捷键速查、检查更新与关于统一收纳至 `❓ 帮助 ▾` 二级下拉菜单，全面支持点击外部与 Esc 键平滑退出，界面专业规整。

### v0.1.115 (2026-09-14)

- **分屏标签单行横向排版与下拉按钮错位修复**：解决分屏模式下打开多个终端（如 ali、PowerShell 7 等）导致标签纵向堆叠、下拉按钮被挤占至新一行的布局缺陷；为分屏标签组设置 Flex 单行排列、底部对齐与隐藏滚动条，多标签横向连续排布且下拉按钮紧随其后；分屏新建标签默认在当前活动分屏窗格内创建。
- **全局快捷键 `Ctrl+,` 与 `Ctrl+W` 深度支持**：在 WebView2 原生键盘加速器与前端按键监听层拦截并支持 `Ctrl+,` 快速唤出偏好设置中心；拦截 Chromium 默认关闭窗口行为，支持 `Ctrl+W` 安全关闭当前激活会话标签。
- **常用 Linux 命令智能补全与通配符模板**：终端内置常用命令模板提示引擎，支持以 `find`、`grep`、`tar`、`docker`、`rsync`、`chmod` 等高频运维命令为前缀动态联想包含通配符与参数示例的高频实用指令模板（如 `find . -name "*.log"`、`find /var/log -type f -mtime -7`），按 Tab / Enter 即可直接完成替换插入。

### v0.1.114 (2026-09-14)

- **RDP 后台静默重连与重试计数持久化**：解决 RDP 重连时强制切换标签抢占焦点的问题，实现后台静默重连且不干扰正在操作的活动会话；重构重试计数器生命周期，按配置持久维护重试状态，彻底修复重试总是停留在第 1 次、无法递增至 5 次上限的循环缺陷。
- **分屏下 RDP 会话共存与视图无缝切换**：解决对 SSH 进行分屏操作时原有 RDP 标签完全消失的问题；分屏模式下完整保留 RDP 标签展示，新增 `🔲 终端分屏` 导航按钮，支持在分屏终端与全尺寸 RDP 桌面间自由切换，且新建或重连 RDP 不再强行销毁分屏模式。
- **RDP 原生系统按键菜单重构**：参考“主题”按钮的原生 Win32 菜单机制，在 RDP 画面处于激活状态时自动调用顶层 Win32 原生 `TrackPopupMenuEx` 渲染系统按键菜单，完美浮动于 ActiveX 原生控件之上，彻底解决 HTML 下拉菜单被原生画面遮挡、裁切或闪烁的问题。
- **快捷键冲突解决与标签数字键切换**：分屏子窗口放大/还原主快捷键调整为 `Alt+M`（兼容保留 `Alt+Z`），彻底消除与 NVIDIA GeForce Experience 等系统全局热键的冲突；全局与终端内部完整支持 `Alt+1..9` 数字键快速直达对应标签页，并支持 `Ctrl+Tab` / `Ctrl+Shift+Tab` 循环切换。
- **SFTP 独立搜索条与一键清空**：将筛选框从密集的工具栏独立抽取为专属 `.sftp-filter-bar` 搜索条，彻底解决工具栏按钮挤压换行的问题；新增放大镜图标与一键清空按钮 (`×`)，输入文字时动态显隐，支持 Esc 快速还原。

### v0.1.113 (2026-09-14)

- **终端内实时关键词查找 (`Ctrl+F`)**：新增无插件双色阶高亮查找引擎（黄蓝色阶区分当前匹配与普通匹配），支持匹配计数（如 `1/5`）、向上/向下查找、大小写敏感切换 (`Aa`)、正则表达式切换 (`.*`)、`F3` / `Shift+F3`、`Alt+C`、`Alt+R`、`Esc` 及全终端回滚缓冲区即时平滑滚动定位。
- **分屏体验优化与 RDP 解除屏蔽**：解除存在 RDP 会话时无法开启分屏的限制；RDP 标签独立保留在顶部标签栏，并新增 `🔲 终端分屏` 导航标签实现分屏终端与 RDP 原生桌面无缝切换；新增单窗口一键放大/还原 (`Alt+Z`) 与悬浮提示条。
- **智能命令片段与参数宏**：支持从命令片段中自动提取 `{{VAR}}` 或 `{{VAR:default}}` 可变参数宏；执行前自动弹出填写对话框，提供实时替换预览、回车快速发送，并使用本地缓存记忆历史输入。
- **RDP 系统按键工具箱与断线重连**：顶部新增 `系统按键 ▾` 按钮与快捷全屏菜单，支持一键注入 `Ctrl+Alt+Del`、`Win 徽标`、`Win+R 运行`、`任务管理器`、`资源管理器`、`命令行 (Cmd)`、`截屏 (PrintScreen)`；意外断开时显示 5 秒倒计时自动重连浮层，支持立即重试与取消，上限 5 次。
- **SFTP 筛选、按键跳转与防覆盖保护**：工具栏新增 `#sftp-filter-input` 实时筛选输入框；焦点在 SFTP 列表时键盘直接键入字符智能跳转定位文件 (Type-to-Jump)，带 800ms 防抖与悬浮指示器；设置中心新增“提示确认（防覆盖保护）”，本地修改保存后触发同步前弹出覆盖确认对话框。
- **企业微信通知自动化集成**：接入 `vm.dapang.wang` 消息网关，发布与迭代完成时自动向企业微信/个人微信推送图文卡片简报。

### v0.1.112 (2026-09-14)

- **RDP 模式主题二级菜单补齐**：修复当标签页显示为 RDP 会话时，点击顶栏“主题”按钮只有扁平的深色/浅色/蓝色选项而没有二级预设子菜单的问题。在原生弹出菜单中完整构建分层级联二级菜单（深色主题、浅色主题、蓝色主题），支持 13 套配色预设（含 默认深色/浅色、One Dark/Light、Dracula、Nord、Monokai Pro、Solarized Dark/Light、Catppuccin Mocha、GitHub Dark/Light、经典海蓝），并绘制对应的色彩指示圆点与当前生效状态对勾标记（`✓`），选择后双向同步更新系统主题与终端配色。

### v0.1.111 (2026-09-14)

- **RDP 弹窗焦点隔离与退出修复**：修复当窗口是 RDP 时打开快速跳转或快捷键速查等弹窗，点击 RDP 画面或按 Esc 无法退出的问题。模态弹窗开启期间原生禁用 RDP 容器避免抢焦与按键穿透，原生遮罩层点击主动联动前端关闭弹窗并归还 WebView2 输入焦点；按 Esc 优先退出顶层弹窗而非误关闭 RDP 会话。
- **云同步 401 令牌失效自动恢复**：捕获“无效或过期的令牌”时自动清理本地残留过期 Token 并解除用户名/密码输入框和登录/注册按钮禁用，提供清晰的状态引导，支持在密码框按回车直接登录。
- **全量弹窗 UI 设计统一**：统一全部 12 个对话框（快速跳转、快捷键速查中心、偏好设置、新建连接、命令收藏、批量执行、云同步、终端诊断、远程预览、端口转发、Action 弹窗）的 10px 统一圆角、高层级柔和投影与毛玻璃遮罩；为快速跳转与快捷键速查全面补齐经典海蓝主题配色。

### v0.1.110 (2026-09-11)

- **快捷键速查中心 (T5-5)**：新增快捷键速查弹窗，支持 `Ctrl+/` 或 `F1` 全局唤出，分组列出全局导航、标签分屏、终端交互与 SFTP 联动全部快捷键，并完全自适应深色/浅色/蓝色主题。
- **工作区连接计数与效率增强 (T5-4)**：工作区下拉筛选框实时显示各工作区的主机数量角标；快速跳转支持搜索并一键切换工作区视图。
- **RDP 动态画质策略与性能预设 (T4-2)**：新增高清质感 (32位色·ClearType·全特效)、平衡自适应 (24位色·平滑字体)、流畅低延迟 (16位色·极速响应·省带宽) 三大画质策略预设，支持一键配置并联动原生参数。
- **快速跳转高亮脱节彻底修复**：消除 CSS 过渡延迟与 1px 外框分离，增加键盘导航锁防止高速滚动时与鼠标指针抢占焦点。

### v0.1.109 (2026-09-11)

- 快速跳转弹窗支持点击外部区域自动退出。
- 快速跳转弹窗在浅色主题下全面适配，ESC 徽标及底栏快捷键指示框改为素雅浅色。
- 本地终端增强未安装 Shell 状态检测，标记 `(未安装)` 状态并提供安装引导。
- 顶栏主题弹窗重构为二级级联菜单（深色/浅色/蓝色），预设自带色块预览且联动切换系统主题，解决色彩冲突。
- 修复点击预设主题时的 `saveSettings is not defined` 异常。
- 云同步与更新服务切换到 `vm.dapang.wang`。

### v0.1.108

- 将云同步、自动更新和发布工具的默认服务地址切换到 `vm.dapang.wang`。
- 旧版本保存的早期内置服务地址会在启动时自动平滑迁移，不影响用户自定义的服务地址。

### v0.1.107

- 修复 RDP 连接失败后原生 ActiveX/OLE 会话仍被保留、导致进程内存占用明显上升的问题。
- 失败事件返回消息循环后立即、幂等释放 RDP 控件、事件订阅、容器窗口和远端桌面缓存；同一标签快速重连时不会误清理新的会话。
- 将 RDP 控件警告与连接失败分开处理，普通警告不再误关闭健康的 RDP 标签。

### v0.1.106

- 统一顶部状态栏的生命周期：SSH/RDP 失败、断开、自动重连和通用错误提示会自动清理，不再永久占据状态栏。
- 更新、分屏、收藏、排序、配置导入导出等操作反馈也改为短暂状态；连接数量和搜索结果仍保留为连接列表状态信息。

### v0.1.105

- 修复 SSH 在 OpenCode/TUI 备用屏幕中断联时错误信息显示在错误位置的问题。
- 传输断开后先由本地终端恢复普通 shell 缓冲区，再追加断开/错误提示；即使远端已经不可写，也不会把恢复序列发送到已断开的 SSH 连接。

### v0.1.104

- 修复执行 `opencode web -h` 等帮助或无头命令后终端历史内容被隐藏的问题。
- 仅对交互式 OpenCode/TUI 命令启用本地备用屏幕；`web`、`serve`、`run`、`acp` 等非交互命令保留在普通 shell 缓冲区。

### v0.1.103

- 将“内容已复制”提示移动到窗口顶部中央，避免出现在右上角时被用户忽略。
- 深色、浅色和蓝色主题分别使用绿色、蓝色和青色提示配色，并同步调整图标、文字、边框和阴影。

### v0.1.102

- 提升“内容已复制”提示的可见性：增加成功图标、明显的成功色、边框和阴影，并适配深色、浅色及蓝色主题。
- 复制提示保留右上角短暂自动消失的交互，同时增加状态语义，便于键盘辅助功能识别。

### v0.1.101

- 从原生窗口层修复 RDP 浮层关闭后的黑框残留：相同裁剪区域不再重复提交，区域变化时先准备内部 ActiveX 控件、最后提交外层容器，并在不擦除黑底的情况下同步重绘。
- 统一覆盖自动隐藏侧栏、设置对话框及其他 WebView 浮层的关闭路径，避免异步布局回调留下旧裁剪区域。
- 合并连接/SFTP 图标的连续悬停事件，并允许重新进入时取消尚未完成的收起操作，减少快速移动鼠标时的卡顿和错误关闭。

### v0.1.100

- 修复自动隐藏侧栏收起时残留黑色框、动画分成两段且明显卡顿的问题。
- RDP 场景改为与原生窗口同步的原子式显示和收回，不再执行跨合成器滑动；SSH、串口和本地终端仍保留轻量平滑动画。

### v0.1.99

- 修复自动隐藏侧栏展开时先出现空白挖孔、再显示实际内容的问题：先显示并绘制侧栏，再让 RDP 让出区域；收起时保持内容到动画结束后再恢复 RDP。

### v0.1.98

- 将连接侧栏阴影改为原生逐像素 Alpha 分层窗口，RDP 桌面保留在阴影下方，不再被 WebView 背景填充成不透明色块。

### v0.1.97

- 放宽连接侧栏阴影的渐变范围并降低分段透明度，使阴影自然淡出，避免在 RDP 右侧显示成突兀的整块色带。

### v0.1.96

- 为连接侧栏增加独立的主题化右侧阴影层，并为 RDP 原生窗口预留对应的可见区域，恢复 RDP 场景下被 ActiveX 覆盖的阴影效果。

### v0.1.95

- 修复连接右键菜单中禁用的“上移/下移”被错误绘制为空白分隔线的问题。
- 优化 SSH/RDP 动态资源栏：首次测量后固定列宽并预留数值变化空间，避免网速、内存等数据刷新时标题栏跳动或内容被截断。

### v0.1.94

- 统一 RDP 场景下连接右键菜单和现有连接菜单的原生弹出层，复用标签菜单的系统阴影、边框和置顶层级，避免菜单被 RDP ActiveX 画面遮挡。
- 修复高延迟 SSH 连接启动多个 SFTP/监控 worker 后可能触发未处理 C++ 异常导致主程序退出的问题，并改进崩溃日志中的异常码记录。

### v0.1.93

- 修复 RDP 原生窗口覆盖 WebView2 时菜单外部阴影被截断的问题。RDP 场景下的全屏控制栏、全屏菜单、现有连接下拉菜单、质量面板和连接右键菜单改用边框与内描边，避免出现局部阴影或残影。

### v0.1.92

- 修复 RDP 场景下 HTML 菜单挖孔与菜单圆角不一致的问题，并增强全屏菜单、连接右键菜单和现有连接下拉菜单的主题边框，避免菜单边缘与远程桌面背景融合。

### v0.1.91

- 重新设计 RDP 场景下的主题菜单和标签右键菜单：改用带当前主题样式的 Win32 顶层自绘弹出菜单，视觉上与连接右键菜单保持一致，并确保始终显示在 RDP 画面上方。

### v0.1.90

- 修复 RDP 场景下主题菜单和 RDP 标签右键菜单被原生远程桌面窗口遮挡的问题：改用带当前主题样式的 Win32 顶层弹出菜单，确保菜单始终显示在 RDP 画面上方。

### v0.1.89

- 重新发布托盘恢复修复版本：确保已处于 `0.1.88` 的客户端可以检测并升级到包含明确窗口显示状态恢复逻辑的最新构建。

### v0.1.88

- 修复托盘恢复时最大化窗口被还原为普通窗口的问题：恢复时显式重新应用 `SW_SHOWMAXIMIZED`/`SW_SHOWNORMAL`，并避免同一次托盘点击重复触发窗口状态切换。

### v0.1.87

- 修复托盘还原与 RDP 菜单挖孔的残留问题：`hideToTray/restoreFromTray` 改用完整 `WINDOWPLACEMENT` 恢复最大化，RDP 右键/主题菜单挖孔精确到右边界且上直角下圆角，下载进度不再误触 `MutationObserver` 导致闪烁。

### v0.1.86

- 修复托盘左键点击时最大化窗口变为窗口化的问题：托盘隐藏前记录 `WINDOWPLACEMENT`，还原时按 `SW_SHOWMAXIMIZED` 恢复最大化。

### v0.1.85

- 修复 RDP 挖孔右侧仍少 1px：改用右边界 `Math.round((r.left+r.width)*d)` 精确计算，确保 `#475569` 边框在所有 DPI 下完整可见。

### v0.1.84

- 修复 RDP 挖孔右侧少 1px、#475569 边框缺失的问题：改用 `Math.round(r.width*d)` 精确匹配菜单宽度，确保右侧边框完整可见。

### v0.1.83

- RDP 挖孔改为上直角下圆角（上半部分直角，下半部分 6px 圆角），符合右键菜单置顶显示的预期，消除 RDP 画面中重复绘制的圆角矩形。
- 进一步优化下载进度 dialog 的 RDP 遮罩触发，避免下载过程中的全屏闪烁。

### v0.1.82

- 修复菜单上方圆角、RDP 内容下方无圆角的不一致：菜单遮罩恢复与菜单一致的 6px 圆角并精确对齐。
- 修复点击更新后下载过程中全屏持续闪烁：对话框内文本与进度条更新不再误触 RDP 遮罩的 MutationObserver。

### v0.1.81

- 修复 RDP 下右键/主题菜单仍被识别为原生菜单位于 RDP 下方、进而在 RDP 画面中重复绘制圆角矩形的问题：菜单遮罩改为无圆角矩形，并排除冻结态对话框对模态判断的干扰。

### v0.1.80

- 进一步修复 RDP 下主题/右键菜单仍显示额外圆角矩形的问题：精确计算遮罩物理像素边界，避免舍入导致的孔洞偏大。

### v0.1.79

- 修复 RDP 下删除连接时全屏变暗及关闭后残留黑影的问题。
- 修复 RDP 下主题切换、右键菜单出现额外圆角矩形边框的问题。
- 修复 RDP 下设置/连接编辑弹窗出现外围边框、而端口转发弹窗正常的问题。

### v0.1.78

- 复制提示改为右上角自动消失的 Toast 弹窗（“内容已复制”），替代左上角常驻状态文字，2 秒自动消失。

### v0.1.77

- 连接列表右键菜单：右键点击连接时该连接会被高亮，便于确认操作对象。
- 终端设置中“选中内容自动复制”默认开启。
- 选中自动复制成功后在状态栏显示提示。

### v0.1.76

- 修复 SFTP 无法浏览根目录的问题：点击“上级目录”到达 `/` 或手动输入 `/` 时，现在会列出真实的根目录内容，不再被静默重定向回远端用户主目录。
- 打开 SFTP 时默认仍进入远端用户主目录，保持原有使用习惯。

### v0.1.75

- RDP 连接接入顶部实时信息栏：显示延迟（有数据时）、连接类型、连接质量、协商分辨率、色深、刷新状态和最近操作；详细质量数据仍可在全屏控制栏中查看。
- RDP 高级配置改为编辑连接时默认展开，不再通过点击标题触发弹窗尺寸和原生遮罩过渡；配置弹窗采用固定高度，内容区域独立滚动。

### v0.1.74

- 修复远端 OpenCode 已退出备用屏幕后，前端重复发送退出序列导致光标回到启动命令位置的问题。
- 收紧 SSH 顶部 CPU、内存、网络、磁盘、运行时间和延迟指标之间的间距，同时保留完整网速文本。

### v0.1.73

- 修复 OpenCode 退出后重复回放终端退出序列，避免 shell 提示符后残留特殊控制输出。
- 扩大 SSH 顶部网络速率显示区域，网络上下行速率不再被窄列的省略号截断。

### v0.1.72

- RDP/SSH 连接成功提示改为短暂状态消息，自动清理，不再永久占用顶部状态栏。
- 确认 VMware Windows 环境应关闭“配置远程桌面连接的 H.264/AVC 硬件编码”；该服务端配置会触发 RDP 图形协议错误 `0x112f`。

### v0.1.71

- 修复切换到已连接 RDP 标签时重复显示“远程桌面已连接”的问题。
- 连接成功消息仅由真实 RDP 状态事件触发，标签切换不再主动播报连接状态。

### v0.1.70

- 修复 RDP 已连接或关闭后顶部仍残留“正在连接远程桌面”提示的问题。
- 将连接提示绑定到具体 RDP 会话，并覆盖成功、失败、断开、重复状态事件、切换标签和关闭标签等清理路径。

### v0.1.69

- 修复 OpenCode 中 Shift+Enter 已换行后又被 xterm.js 追加普通 Enter 的问题，增加事件默认行为阻止和重复回车抑制。

### v0.1.68

- SSH 中运行 OpenCode 时，Shift+Enter 发送 CSI-u 换行序列（`ESC[13;2u`），不会提交当前输入；普通 SSH 命令行的 Enter 行为保持不变。

### v0.1.67

- 修复云端历史版本改名和删除请求的 `seq` 参数兼容性，兼容旧客户端和字符串形式的版本序号。
- 修复更新包 SHA-256 校验在大文件下载中的客户端哈希实现，改用 Windows BCrypt 并保留可移植回退实现。

### v0.1.66

- 云端历史版本弹窗改为固定高度，版本过多时仅列表区域滚动。
- 历史版本支持自定义名称、删除和恢复，删除操作需要确认。
- 云同步服务增加历史版本改名和删除接口，并兼容已有数据库自动迁移。

### v0.1.65

- 标签栏左侧圆点按连接类型显示颜色：SSH、RDP、串口分别使用设置中的类型颜色。
- 连接中使用颜色呼吸效果，错误状态使用外圈提示，类型颜色保持不变。
- 部署云同步历史版本接口，修复历史版本提示“接口不存在”的问题。

### v0.1.64

- 修复打开新 RDP 连接时误显示上一个 RDP 桌面的状态污染问题。
- 新 RDP 连接建立期间立即隐藏旧桌面并显示“正在连接远程桌面”提示；连接成功后再显示新桌面。
- 保留已连接 RDP 标签之间的原生画面保活切换，避免无意义的重连和黑屏闪烁。

### v0.1.63

- 拖动连接卡片时自动切换到“手动顺序”，无需先单独修改排序选项。
- 右键“上移/下移”在分组视图中按当前分组工作，并同样支持自动切换手动顺序。

### v0.1.62

- 分组视图支持手动拖动排序。
- 按工作区分组时，拖入其它工作区会同步更新连接归属。
- 按工作区/连接类型分组时，跨连接类型拖动会被拒绝，仅允许放入同类型区域。

### v0.1.61

- 修复连接卡片的排序/分组菜单在 RDP 原生画面上点击后立即关闭、导致选项不生效的问题。
- 优化排序/分组浮层为统一的圆角菜单样式，并保留 RDP 原生窗口的遮挡区域同步。

### v0.1.60

- 连接卡片默认按连接类型分组，组内按名称排序；支持名称、地址、最近连接、手动顺序以及工作区/类型分组切换。
- 新增连接视图菜单，手动顺序模式继续支持拖动和右键上移/下移。
- 连接标签默认跟随连接类型显示（SSH、RDP、串口）；已有自定义标签保持不变，并支持在设置中修改三类连接的标签颜色。

### v0.1.59

- 连接卡片支持拖动排序，排序结果会保存到本地连接配置。
- 连接卡片右键菜单增加“上移”和“下移”，在筛选/工作区视图中按当前可见连接排序。
- 拖动和右键排序共用后端持久化接口，云端导出时会保留连接顺序。

### v0.1.58

- 修复 RDP 退出所有屏幕全屏时先以 `320x240` 建立再异步调整到正常尺寸的问题。
- 进入/退出全屏时复用已保存的 RDP 容器布局，避免重复触发排版和分辨率刷新。
- 按 0.1.54 的行为基线保持普通窗口单屏嵌入，仅在所有屏幕全屏时建立多屏会话。

### v0.1.57

- 恢复 0.1.54 的 RDP 显示流程：普通连接保持当前窗口内的单显示器嵌入显示，进入“所有显示器全屏”时才创建容器托管的多显示器会话，退出后恢复单显示器会话。
- 将显示模式切换集中到受控的 RDP 会话替换流程，避免旧 ActiveX 控件与新控件同时占用画面。

### v0.1.56

- 修复多显示器 RDP 连接初始打开时误呈现为全屏的问题：连接建立时明确保持 `FullScreen=FALSE`，普通状态只显示在 MasterTerm 当前窗口内；点击全屏后才扩展本地容器到目标显示器范围，且不重连远程会话。

### v0.1.55

- RDP 多显示器模式在连接建立时完成协商，全屏切换不再重建 ActiveX 控件或重新连接远程会话。
- 保持远端 Windows 已打开应用的窗口位置，避免进入/退出双屏全屏时被重新布局。
- 多显示器协商失败时回退到当前显示器全屏，并保留现有连接。

### v0.1.54

- 重做 RDP“所有显示器”全屏呈现：改用 `ContainerHandledFullScreen`，由 MasterTerm 的无边框原生容器直接覆盖本机显示器组合，ActiveX 填满容器并保留多显示器拓扑。
- 不再调用会额外创建 `UIMainClass` 的 ActiveX 原生全屏路径，解决空白 MasterTerm 窗口、黑色 RDP 外壳以及必须滚动才能查看双屏桌面的问题；新增容器、滚动条和远端拓扑联合校验。

### v0.1.53

- 修复混合分辨率双屏环境中，RDP 已正确协商两个远端显示器但原生 `UIMainClass` 全屏宿主仍只覆盖主屏的问题；进入全屏后会按本机真实虚拟桌面边界校正该宿主窗口，同时保留 RDP 的逐显示器拓扑。
- 修复多屏呈现校验失败或退出全屏时，程序在 ActiveX 全屏事件回调栈中同步销毁并重建控件而导致的卡死；单屏会话恢复改为事件完成后的延迟重建。

### v0.1.52

- RDP 控件改为优先使用 Windows 已注册的最新版本（从 `MsTscAx.13` 向旧版回退），避免旧版控件在混合分辨率/缩放的显示器上把多屏桌面放进单屏滚动视口。
- 多显示器全屏增加呈现校验：检查远端拓扑、横纵滚动条和本地全屏窗口覆盖范围；检测到错误呈现时自动安全退出并写入详细诊断日志。
- 原生 RDP 全屏期间将 MasterTerm 主窗口从任务栏和 Alt+Tab 临时移除，退出全屏后恢复，避免出现白色主窗口和两个任务栏缩略图。

### v0.1.51

- 重做 RDP“所有显示器”全屏生命周期：主窗口不再扩展为 4480×1440 的虚拟桌面，`UseMultimon` 在连接前协商，连接完成后再由 mstscax 进入原生多屏全屏。
- 明确设置 `ContainerHandledFullScreen=0`，让 RDP 控件创建独立的显示器呈现面；自定义控制栏改为独立置顶窗口，退出全屏后恢复进入前的窗口位置与状态。

### v0.1.50

- 修复“所有显示器全屏”仅闪烁一帧的问题：多屏全屏期间隐藏 WebView2 白色呈现层，改由原生 mstscax 独占虚拟屏客户区，工具栏和菜单继续使用原生窗口显示。

### v0.1.49

- RDP 高级配置将全屏范围明确为“当前显示器”或“所有显示器”两种模式；旧版 `useMultimon` 的 0/1 配置继续兼容，默认使用所有显示器。

### v0.1.48

- 修复双屏 RDP 全屏白屏：多显示器连接使用主屏初始尺寸并保留 `UseMultimon` 拓扑，避免把虚拟桌面宽度当成单屏分辨率反复写入；同时避免强制重绘清空 mstscax 的多屏内部呈现面。
- 增加多屏会话诊断日志（远端显示器数量、远端边界框、布局匹配状态）以及全屏退出窗口矩形恢复，避免退出全屏后主窗口继承 4480 像素的虚拟屏宽度。

### v0.1.47

- 延续双屏 RDP 全屏白屏与窗口尺寸修复，并清理单屏诊断日志中的旧多屏接口名称，避免实际未调用的接口造成误导。

### v0.1.46

- 修复双屏 RDP 全屏切换后白屏的问题：多显示器拓扑仅在连接建立前协商，连接建立后的布局刷新不再调用会返回 `E_POINTER` 的同步接口；同时阻止全屏过渡期间的 DPI 尺寸消息覆盖普通窗口位置。

### v0.1.45

- 修复 RDP ActiveX 在已连接状态下无法切换 `UseMultimon` 的问题。
  进入或退出双屏全屏时会重建嵌入式控件，并在建立连接前应用多显示器模式，避免回退成单一的超宽虚拟屏幕。

### v0.1.44

- 修复 RDP 双屏全屏时将两个显示器错误协商为一块虚拟超宽屏的问题。
  多显示器全屏现在使用 ActiveX 的布局同步接口保留本机显示器拓扑，单屏窗口和退出全屏仍按窗口尺寸刷新。

### v0.1.43

- RDP 全屏原生控制栏新增“实际质量”面板，显示每个会话的色深、当前分辨率、SmartSizing、性能标志、连接类型、带宽检测、协议模式、完整刷新次数和最近 HRESULT。
- 增加编码/AVC 状态说明：嵌入式 `mstscax` 接口只能确认客户端协议模式，无法直接回读服务端最终协商的 AVC/H.264 编码，避免把 `FullMode` 误认为 AVC 已启用。
- 质量状态通过 `rdp.quality` 事件同步到前端，并保留无原生覆盖层时的 WebView2 备用查看面板；新增 T4-1 专项回归测试。

### v0.1.42

- 完善中文和特殊路径支持：主程序与 SFTP Worker 启用 Windows 长路径，保留 UTF-8 文件名、Emoji、空格、括号和递归目录的路径信息，并增加原生路径文本往返测试。

### v0.1.41

- 完善 SFTP 传输中心：增加按文件名、路径、方向和错误原因筛选，支持批量重试失败/取消任务；传输详情补充本地路径、远程路径和断点信息，并让删除、排序等队列操作立即持久化。

### v0.1.40

- SFTP 同名文件冲突提示增加本机/远端大小与修改时间比较信息，支持比较后选择跳过、自动重命名、覆盖或全部应用。

### v0.1.39

- SFTP 目录上传/下载增加逐文件断点续传：重启或失败重试时按已有文件大小继续，完整文件跳过，并可选用 SHA-256 校验；传输队列会保存每个文件的进度检查点。

### v0.1.38

- 将 RDP 显示与画质中的二值选项改为直观的复选框，默认启用字体平滑、桌面合成、视觉样式等显示效果，并默认使用全屏多显示器模式。

### v0.1.37

- 修复云同步上传遗漏工作区配置的问题；上传和下载时都会同步工作区名称及连接归属。

### v0.1.36

- 增加 NSIS 正式安装包构建流程，支持安装到 `Program Files\MasterTerm`、开始菜单/桌面快捷方式和卸载。

### v0.1.35

- 发布当前主分支最新版本，包含 RDP 质量配置回读、恢复默认设置入口以及相关界面修复。

### v0.1.34

- RDP“恢复默认设置”按钮移动到连接编辑器底部，与“取消/保存”操作保持在同一栏，并仅在 RDP 连接中显示。

### v0.1.33

- RDP 高级配置增加“恢复 RDP 默认设置”按钮，可清除当前连接的高级覆盖项并回到 Windows RDP 控件默认行为。
- 连接日志增加颜色深度、智能缩放、性能标志、连接类型、带宽检测和协议模式的实际回读值，便于确认“连接速度”是否被控件接受。
- 帧缓冲重定向仅对本机回环连接启用，不再对 VMware/PVE 等私网 RDP 连接强制使用该图形路径。

### v0.1.32

- RDP 连接上展开或收起高级配置时，先生成目标状态的 WebView2 渲染快照，再更新原生 RDP 裁剪区域，避免两个合成器提交不同步时短暂露出黑色矩形。
- 原生端确认新的弹窗区域已生效后才释放快照；处理期间锁定重复点击，避免交叉切换破坏弹窗与 RDP 遮罩状态。

### v0.1.31

- 高级配置切换改为由原生宿主一次性修改 WebView 内容、读取目标边界并应用 RDP 遮罩，消除两个独立渲染层之间的异步先后变化。
- 更新请求现在会等待原生遮罩真正应用后再确认，并阻止切换尚未完成时的重复点击。

### v0.1.30

- 高级配置的展开/收起改为在同一次点击处理中同步提交内容、弹窗尺寸和原生 RDP 遮罩，消除先后变化的视觉错位。
- 保留程序化切换和辅助功能触发时的遮罩同步兜底。

### v0.1.29

- 修复新建 RDP 连接时展开高级配置后，原生 RDP 画面遮住弹窗标题和顶部字段的问题。
- 修复编辑 RDP 连接时收起高级配置后，弹窗周围残留上一次大尺寸遮罩背景的问题。
- 每次打开连接编辑器时默认收起高级配置，避免继承上一次编辑状态。

### v0.1.28

- 原生 RDP 遮罩现在读取每个 HTML 弹窗和菜单的真实圆角半径，并使用圆角区域挖孔，修复所有弹窗圆角外残留矩形背景的问题。
- 合并 RDP 高级配置弹窗的定位规则，展开后通过固定视口和自动外边距保持居中，顶部、底部及滚动区域均保持可访问。

### v0.1.27

- 修复连接配置弹窗圆角外的矩形背景，并保持高级配置展开前后始终垂直居中。
- 减少 RDP 标签切换和全屏进出时重复的显示分辨率协商与完整画面刷新，降低连续黑屏闪烁。

### v0.1.26

- 修复 RDP 高级配置展开后的滚动边界：展开时回到表单顶部，顶部和底部内容均可通过同一滚动区域完整访问。
- 修复设置和连接配置弹窗圆角外仍显示矩形背景的问题，并固定高级配置弹窗在视口内定位。

### v0.1.25

- 固定“终端设置”弹窗的可视高度为紧凑尺寸，标题栏和底部操作栏保持可见，设置内容在中间区域独立滚动，避免设置页过高遮挡主界面。

### v0.1.24

- RDP 高级配置展开时固定编辑窗口可视高度，超出内容仅在表单主体内滚动，避免弹窗被原生 RDP 层切开。
- 补充 RDP 服务器画质差异排查说明：服务器系统版本、虚拟显示驱动、组策略和 RDP 编码策略都会影响字体与局部画面质量。

### v0.1.23

- 修复本机和局域网 RDP 首帧画面发糊：启用稳定的帧缓冲路径，并在连接完成后分阶段刷新完整画面和当前分辨率。
- “全屏时使用所有显示器”改为进入 RDP 全屏时才启用；普通窗口连接仍使用单显示器，退出全屏后恢复原窗口。
- 配置弹窗按内容自适应，长表单在主体区域滚动；RDP 高级配置按显示画质、输入重定向、连接重连分组。

### v0.1.22

- RDP 全屏原生控制栏跟随深色、浅色和蓝色主题切换，菜单和按钮保持一致的配色。
- 修复 RDP 设置“高级配置”内容被截断的问题：表单区域独立滚动，底部操作栏始终可用。
- 扩展 RDP 高级配置：连接速度、桌面背景、字体平滑、桌面合成、完整窗口拖动、菜单动画、视觉样式、光标效果，以及全屏使用所有屏幕。
- 新增配置会保留“默认”选项；未设置时继续使用 Windows RDP 控件或服务器的默认行为。

### v0.1.21

- 将 `127.0.0.1`、`10.x.x.x`、`172.16-31.x.x`、`192.168.x.x` 等本机或私网 RDP 地址识别为局域网连接，默认使用 32 位色和 LAN 高画质传输策略。
- RDP 连接和登录完成后，会按当前实际可视区域重新提交一次动态分辨率并请求完整画面，改善首帧模糊、刷新后才变清晰的问题。
- 固定/取消固定左侧连接栏或完成列宽调整后，所有已打开的 RDP 会话会统一适配新的可视区域；拖动过程中不会连续协商分辨率。
- 多个 RDP 标签保持各自独立连接，切换标签时复用已渲染画面，并完善断线提示、原生窗口层级、弹窗遮罩和全屏控制栏。
